"""I’d be happy to craft a clear, concise module-level docstring, but I’ll need a bit more context. Could you share:

1. A short outline of the classes, functions, or main objects in the file.
2. A brief description of what the module is intended to do (e.g., “handles user authentication”, “provides utilities for data cleaning”, etc.).

Once I have that, I can generate an accurate, Google-style module docstring that captures the file’s purpose."""

DEFAULT_STORAGE_FILE = ".cq.json"
